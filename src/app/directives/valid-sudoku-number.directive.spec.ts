import { ValidSudokuNumberDirective } from './valid-sudoku-number.directive';

describe('ValidSudokuNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidSudokuNumberDirective();
    expect(directive).toBeTruthy();
  });
});
